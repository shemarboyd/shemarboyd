<#
.SYNOPSIS
    This PowerShell script enables powershell Transcription
.NOTES
    Author          : Shemar Boyd
    LinkedIn        : https://www.linkedin.com/in/shemar-boyd-a23388150/
    GitHub          : github.com/shemarboyd
    Date Created    : 2026-03-26
    Last Modified   : 2026-03-26
    Version         : 1.0
    CVEs            : N/A
    Plugin IDs      : N/A
    STIG-ID         : WN11-CC-000327

.TESTED ON
    Date(s) Tested  : 3/27/2026
    Tested By       : shemarboyd
    Systems Tested  : Windows 11 vm
    PowerShell Ver. : 5.1.26100.7920

.USAGE
    Put any usage instructions here.
    Example syntax:
    PS C:\> .\__remediation_template(STIG-ID-WN11-CC-000327).ps1 
#>

# WN11-CC-000327
# Enable PowerShell Transcription

$registryPath = "HKLM:\SOFTWARE\Policies\Microsoft\Windows\PowerShell\Transcription"
$outputFolder = "C:\PSTranscripts"

# Create transcript folder if it doesn't exist
if (-not (Test-Path $outputFolder)) {
    New-Item -Path $outputFolder -ItemType Directory -Force | Out-Null
}

# Create registry path if it doesn't exist
if (-not (Test-Path $registryPath)) {
    New-Item -Path $registryPath -Force | Out-Null
}

# Enable PowerShell Transcription
New-ItemProperty -Path $registryPath -Name "EnableTranscripting" -Value 1 -PropertyType DWord -Force | Out-Null

# Include invocation headers (recommended / often checked)
New-ItemProperty -Path $registryPath -Name "EnableInvocationHeader" -Value 1 -PropertyType DWord -Force | Out-Null

# Set transcript output directory
New-ItemProperty -Path $registryPath -Name "OutputDirectory" -Value $outputFolder -PropertyType String -Force | Out-Null

Write-Host "WN11-CC-000327 remediated: PowerShell Transcription enabled."
Write-Host "Transcript folder: $outputFolder"
